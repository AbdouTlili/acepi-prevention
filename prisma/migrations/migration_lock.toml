# Please do not edit this file manually
# It should be added in your version-control system (e.g., Git)
# NOTE: kept as postgresql because migrations target GCP Cloud SQL.
# Local dev uses SQLite via prisma/schema.prisma (provider = "sqlite") + a separate db.
provider = "postgresql"
